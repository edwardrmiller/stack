<h2>Add a link to the <%= @category.name %> category<h2>

<%= simple_form_for [@category, @link] do |f| %>

	<%= f.input :name %>
	<%= f.input :description %>
	<%= f.input :url %>

	<%= f.button :submit %>

<%end %>